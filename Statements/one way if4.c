#include<stdio.h>

int main()
{
	int year;
	
	printf("weka mwaka");
	
	scanf("%d",&year);
	
if(year%400==0 && year%100!=0 || year%4==0);
  
  {
  	printf("%d is a leap year",year);
  }
  
  return 0;
}
